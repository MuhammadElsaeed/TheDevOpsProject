name: CI — Backend

on:
  push:
    branches: [ dev, main ]
    paths:
      - 'apps/backend/**'
      - '.github/workflows/backend-ci.yml'
  pull_request:
    branches: [ dev, main ]
    paths:
      - 'apps/backend/**'
      - '.github/workflows/backend-ci.yml'
  workflow_dispatch: {}

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    env:
      IMAGE_REGISTRY: ${{ secrets.ARTIFACT_REGISTRY }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install dependencies
        run: |
          cd apps/backend
          if [[ -f package-lock.json ]]; then
            npm ci
          else
            npm install
          fi

      - name: Run unit tests
        run: |
          cd apps/backend
          npm test

      - name: Authenticate to Google Cloud using Workload Identity Federation
        uses: google-github-actions/auth@v1
        with:
          workload_identity_provider: "projects/931968123240/locations/global/workloadIdentityPools/github-actions-pool/providers/github-provider"
          service_account: "github-actions-sa@thedevopsproject.iam.gserviceaccount.com"

      - name: Configure gcloud
        uses: google-github-actions/setup-gcloud@v1
        with:
          project_id: thedevopsproject

      - name: Verify authenticated account and permissions
        run: |
          echo "Active gcloud account:"
          gcloud auth list --filter=status:ACTIVE --format="value(account)" || true
          echo "gcloud config account:"
          gcloud config get-value account || true
          echo "Check project IAM bindings for the SA (may be empty if API restricted):"
          gcloud projects get-iam-policy "$CLOUDSDK_CORE_PROJECT" \
            --flatten="bindings[]" \
            --filter="bindings.members:serviceAccount:github-actions-sa@thedevopsproject.iam.gserviceaccount.com" \
            --format="table(bindings.role,bindings.members)" || true

      - id: build
        name: Build Docker image
        run: |
          IMAGE="${IMAGE_REGISTRY}/demo-backend:${GITHUB_SHA}"
          echo "image=$IMAGE" >> $GITHUB_OUTPUT
          docker build -t "$IMAGE" apps/backend
          echo "$IMAGE"

      - name: Push Docker image to Artifact Registry
        run: |
          gcloud auth configure-docker --quiet
          IMAGE="${{ steps.build.outputs.image }}"
          docker push "$IMAGE"

      - name: Tag and push env alias
        run: |
          IMAGE="${{ steps.build.outputs.image }}"
          if [[ "${GITHUB_REF}" == "refs/heads/dev" ]]; then
            ALIAS="dev-latest"
          else
            ALIAS="prod-latest"
          fi
          IMAGE_ALIAS="${IMAGE_REGISTRY}/demo-backend:${ALIAS}"
          docker tag "$IMAGE" "$IMAGE_ALIAS"
          docker push "$IMAGE_ALIAS"
          echo "Pushed alias $IMAGE_ALIAS"

      - name: Scan image with Trivy
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ steps.build.outputs.image }}

      - name: Helm lint templates
        run: |
          helm version
          helm lint apps/backend/chart --set image.registry=${IMAGE_REGISTRY}
